name: something-else
on:
  push:
    branches:
    - '*'

jobs:
  micro_checks:
    name: Check thing
    strategy:
      fail-fast: true
      matrix:
        include:
          - os: windows-latest
          - os: ubuntu-latest
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v4
    - run: cargo run
